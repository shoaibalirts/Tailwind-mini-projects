'use client';
import EmailSubscribeCard from "./EmailSubscribeCard";

export default function EmailSubscribeComp() {
  return (
    // Background Container
    <main className="flex flex-col items-center justify-center min-h-screen bg-zinc-700">
      {/* Card */}
      <EmailSubscribeCard />
      
    </main>
  );
}
